# Pull Request: Переделка страницы расписания - карточки людей + split-layout

## 🎨 Описание изменений

Полная переработка страницы расписания с заменой таблицы на карточки людей и внедрением split-layout дизайна.

## ✨ Что нового

### 1. 👤 Карточки людей вместо таблицы (левая панель)
- ✅ **Аватар** с первыми буквами имени (круглый, цвет команды)
- ✅ **ФИО** как заголовок карточки
- ✅ **Команда** с иконкой Users
- ✅ **Даты From - To** (автоматически вычисляются из проектов)
- ✅ **Утилизация в часах** (сумма hc × 40h всех проектов)
- ✅ Вертикальный список с hover эффектами
- ✅ Фиксированная ширина 320px

### 2. 🎛️ Кнопки масштаба НАД диаграммой
- ✅ Перемещены **Неделя | Месяц | Полугодие** над Gantt
- ✅ Активная кнопка подсвечивается синим
- ✅ Размещены в правой панели (над диаграммой)
- ✅ При клике меняется масштаб временной шкалы

### 3. ⚡ Split-layout с горизонтальным скроллом
- ✅ **Левая панель** (320px) - фиксированная, вертикальный скролл
- ✅ **Правая панель** (flex: 1) - горизонтальный + вертикальный скролл
- ✅ Встроенная таблица библиотеки **полностью скрыта**
- ✅ Адаптивный дизайн для мобильных (панели друг под другом)

### 4. 🧹 Упрощение UI
- ✅ Убраны кнопки "По командам / По проектам" (упрощение навигации)
- ✅ Убран блок `control-panel` (теперь не нужен)
- ✅ Чистый минималистичный интерфейс

## 📁 Новые/измененные файлы

**Новые:**
- `src/components/PersonCard.tsx` - компонент карточки человека (72 строки)

**Измененные:**
- `src/components/SchedulePage.tsx` - split-layout, карточки слева, Gantt справа
- `src/premium-glass.css` - стили для split-layout, person-card, gantt-zoom-controls

## 🎯 Технические детали

**Компонент PersonCard.tsx:**
```tsx
- getInitials(name) → "ИИ" (первые буквы имени)
- getDateRange(executor) → { start, end } (min/max даты из проектов)
- getTotalHours(executor) → number (сумма hc × 40h)
- formatDate(date) → "05 янв" (локализованный формат)
```

**CSS структура:**
```css
.schedule-split-layout       → flex контейнер
.people-cards-panel          → левая панель 320px, overflow-y
.gantt-panel                 → правая панель flex: 1, overflow-x
.gantt-zoom-controls         → кнопки масштаба над диаграммой
.person-card                 → карточка человека с hover
.person-avatar               → круглый аватар с буквами
```

**Скрытие встроенной таблицы:**
```tsx
listCellWidth="0px"                    // в компоненте Gantt
.gantt-container ._3ZbQT { display: none !important; }  // в CSS
.gantt-container ._3_ygE { display: none !important; }  // в CSS
```

## 📋 Чек-лист тестирования

- [x] Сборка проекта проходит без ошибок
- [x] Карточки людей отображаются корректно
- [x] Аватары с правильными буквами и цветами команд
- [x] Даты From-To вычисляются правильно
- [x] Утилизация (часы) считается корректно
- [x] Кнопки масштаба работают (Неделя/Месяц/Полугодие)
- [x] Левая панель фиксированная, вертикальный скролл
- [x] Правая панель горизонтально скроллится
- [x] Встроенная таблица библиотеки скрыта
- [x] Адаптивный дизайн на мобильных устройствах
- [x] Hover эффекты на карточках работают

## 🚀 Как тестировать

```bash
npm install
npm run dev
```

Откройте http://localhost:3000/ и проверьте:

1. **Левая панель:**
   - 6 карточек людей (по командам: Разработка, Дизайн, QA)
   - Аватары с буквами и цветами команд
   - ФИО, команда, даты, утилизация

2. **Правая панель:**
   - Кнопки масштаба НАД диаграммой
   - Gantt диаграмма горизонтально скроллится
   - Цветные блоки проектов видны

3. **Адаптивность:**
   - На мобильных панели друг под другом
   - Скроллы работают корректно

### 5. 🐛 Исправлены баги визуализации
- ✅ Кнопки масштаба больше не накладываются на заголовок "2026"
- ✅ Шкала времени отображается корректно (месяцы не наложены)
- ✅ Заголовки месяцев читаемы и не перекрываются

## 📊 Статистика изменений

**Коммиты:**
- `d3fa264` - Переделать страницу расписания: карточки людей + split-layout
- `eb17528` - Исправить баги визуализации Gantt диаграммы

**Файлы:**
- +1 новый файл (PersonCard.tsx)
- ~2 измененных файла (SchedulePage.tsx, premium-glass.css)
- +286 строк добавлено
- -64 строк удалено

**Размер сборки:**
- CSS: 16.59 kB (было 14.87 kB, +1.72 kB)
- JS: 195.59 kB (было 195.47 kB, +0.12 kB)

---

**Версия:** 3.0.0 Premium
**Дизайн:** Plane/Float/Apple-inspired + Split-layout
**Иконки:** Lucide React (Users, Calendar, Clock)
